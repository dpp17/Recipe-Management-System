spring.profiles.active = dev

environment = COMMON

spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=${URL}/Recipes?createDatabaseIfNotExist=true
spring.datasource.username=${UserName}
spring.datasource.password=${Password}

#